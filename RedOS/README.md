# 🛠️ Инструкция по установке Red OS 8 и настройке окружения разработчика

Данное руководство описывает полный цикл подготовки рабочей станции: от установки ОС до настройки стека **C++ / Qt / PostgreSQL / Git**.

---

## 📋 Содержание
1. [Подготовка и установка ОС](#1-подготовка-и-установка-ос)
2. [Полное обновление системы](#2-полное-обновление-системы)
3. [Настройка Git и SSH](#3-настройка-git-и-ssh)
4. [Инструменты разработки](#4-инструменты-разработки)
5. [Установка и настройка Qt 5](#5-установка-и-настройка-qt-5)
6. [Установка и настройка PostgreSQL 15](#6-установка-и-настройка-postgresql-15)
7. [Контрольный чек‑лист](#7-контрольный-чек‑лист)

---

## 1. Подготовка и установка ОС

### 1.1. Образ и загрузочная флешка
1. **Скачайте ISO‑образ** Red OS 8 (редакция «Рабочая станция»).
2. **Запишите образ на USB‑накопитель** (не менее 8 ГБ).

#### 🐧 Под Linux
> ⚠️ **Внимание:** Команда `dd` полностью стирает данные на флешке. Будьте осторожны с выбором устройства.

```bash
# Замените /dev/sdX на путь к вашей флешке (проверьте через lsblk)
sudo dd if=RedOS-8.0-Workstation.iso of=/dev/sdX bs=4M status=progress oflag=sync
```

#### 🪟 Под Windows
Используйте **Rufus** или **BalenaEtcher** в режиме *«DD / образ диска»*.

### 1.2. Настройка BIOS/UEFI
*   Отключите **Secure Boot**.
*   Установите приоритет загрузки с **USB‑устройства** выше, чем с HDD/SSD.

### 1.3. Установка Red OS
1. Загрузитесь с подготовленной флешки.
2. В меню загрузчика выберите **«Установка Red OS»**.
3. Следуйте мастеру установки (язык, раскладка, часовой пояс).
4. **Разметка диска:**
    *   Для чистой установки: *«Автоматическая разметка»*.
    *   При необходимости: *«Вручную»* (создайте разделы `/`, `/home`, `swap`).
5. **Создание пользователей:**
    *   Задайте пароль **root**.
    *   Создайте рабочего пользователя и **обязательно** отметьте пункт:
        > ☑️ *Сделать администратором (добавление в группу wheel)*.
6. Завершите установку и перезагрузите систему.

---

## 2. Полное обновление системы

После первой загрузки необходимо обновить пакетную базу.

### 2.1. Очистка кэша и обновление метаданных
```bash
sudo dnf clean all
sudo dnf makecache
```

### 2.2. Обновление пакетов
```bash
sudo dnf -y update
```

> ℹ️ **Важно:** После обновления ядра необходима перезагрузка.
> ```bash
> sudo reboot
> ```

---

## 3. Настройка Git и SSH

### 3.1. Установка и базовая конфигурация Git
```bash
sudo dnf -y install git

git config --global user.name  "Имя Фамилия"
git config --global user.email "user@example.com"
git config --global core.editor "nano"
```

### 3.2. Генерация SSH‑ключа
Для безопасной работы с внутренним Git‑сервером:

```bash
ssh-keygen -t ed25519 -C "user@example.com"
```
*   **Закрытый ключ:** `~/.ssh/id_ed25519`
*   **Открытый ключ:** `~/.ssh/id_ed25519.pub`

Вывод открытого ключа для копирования в профиль (например, GitLab):
```bash
cat ~/.ssh/id_ed25519.pub
```

### 3.3. Проверка соединения
```bash
ssh -T git@git.0825.vniief.ru
```

---

## 4. Инструменты разработки

Установка компиляторов (GCC), отладчика и систем сборки.

```bash
# Группа инструментов разработки
sudo dnf groupinstall -y "Development Tools"

# Дополнительные утилиты (CMake, Ninja, GDB)
sudo dnf install -y gcc-c++ cmake ninja-build gdb
```

---

## 5. Установка и настройка Qt 5

### 5.1. Установка Qt Creator и библиотек
```bash
sudo dnf install -y qt-creator libdrm \
    qt5-qtbase-devel \
    qt5-qtdeclarative-devel \
    qt5-qtquickcontrols2 \
    qt5-qtquickcontrols2-devel \
    qt5-qtsvg-devel \
    qt5-qttools \
    qt5-qttools-libs-designer \
    qt5-qttools-devel
```

### 5.2. Настройка Qt Creator
1. Запустите **Qt Creator**.
2. Перейдите в **Инструменты** → **Параметры** → **Сборка и запуск**.
3. **Вкладка Qt Versions:**
    *   Добавьте версию Qt, указав путь к qmake: `/usr/bin/qmake-qt5`.
4. **Вкладка Компиляторы (Compilers):**
    *   Убедитесь, что определён компилятор GCC (C++) с путём `/usr/bin/g++`.
5. **Вкладка Комплекты (Kits):**
    *   Выберите комплект по умолчанию или создайте новый.
    *   Укажите:
        *   **Qt version:** Qt 5.x (qmake-qt5)
        *   **C++ compiler:** GCC (/usr/bin/g++)
        *   **Debugger:** gdb

---

## 6. Установка и настройка PostgreSQL 15

### 6.1. Установка сервера и клиента
```bash
sudo dnf install -y postgresql15-server postgresql15
```

### 6.2. Инициализация базы данных
```bash
sudo /usr/pgsql-15/bin/postgresql-15-setup initdb
```

### 6.3. Запуск и автозагрузка
```bash
sudo systemctl enable --now postgresql-15
```

### 6.4. Настройка пароля администратора
Войдите под пользователем `postgres`:
```bash
sudo -u postgres psql
```

В консоли `psql` выполните SQL-запрос:
```sql
ALTER USER postgres WITH ENCRYPTED PASSWORD 'Ваш_Сложный_Пароль';
\q
```

---

## 7. Контрольный чек‑лист

Запустите команды для финальной проверки окружения:

| Компонент | Команда проверки | Ожидаемый результат |
| :--- | :--- | :--- |
| **Права root** | `sudo whoami` | `root` |
| **Git** | `git --version` | `git version 2.x.x` |
| **Компилятор** | `g++ --version` | `g++ (GCC) x.x.x` |
| **Qt / qmake** | `qmake-qt5 -v` | `Using Qt version 5.x.x` |
| **PostgreSQL** | `systemctl status postgresql-15` | `active (running)` |
| **SSH‑ключ** | `ls -l ~/.ssh/id_ed25519` | файл существует |

### 📱 Дополнительно
Для установки Telegram Desktop:
```bash
sudo dnf install -y telegram-desktop
```

---
*Документация актуальна для: **Red OS 8.0***
*Последнее обновление: **2026‑01‑15***
```
