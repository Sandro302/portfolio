# 📡 Engineering Pipeline Calculation System API

REST API для системы расчёта и визуализации параметров трубопровода.

## 🚀 Документация

### ⭐ Выберите удобный вариант:

- **[🔧 Swagger UI + ReDoc (Комбо)](full.html)** — Обе в одном файле с вкладками
  - Try It Out - прямое тестирование эндпоинтов
  - Красивый обзор документации
  - Как в SwaggerEditor!

Или отдельно:

- **[📚 ReDoc](index.html)** — Только чтение
- **[🔧 Swagger UI](swagger.html)** — Только тестирование

---

## 📖 Быстрый обзор

### Base URL

```bash
curl -X POST https://api.example.com/v1/projects
```


### Основные эндпоинты

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/projects` | Список проектов |
| POST | `/projects` | Создать проект |
| POST | `/projects/import/olga` | Импортировать OLGA |
| POST | `/projects/{id}/calculations` | Запустить расчёт |
| GET | `/projects/{id}/results` | Получить результаты |
| POST | `/projects/{id}/export` | Экспорт в CSV |

## 🔍 Примеры

### Создание проекта
```bash
curl -X POST https://api.example.com/v1/projects \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Pipeline A",
    "description": "Main pipeline"
  }'
```
## Response:

```json
{
  "id": "proj_123",
  "name": "Pipeline A",
  "status": "ready",
  "createdAt": "2026-01-14T00:00:00Z"
}
```

### Импорт OLGA

```bash
curl -X POST https://api.example.com/v1/projects/import/olga \
  -F "name=Pipeline" \
  -F "files=@project.opi"
```

### Запуск расчёта

```bash
curl -X POST https://api.example.com/v1/projects/proj_123/calculations \
  -H "Content-Type: application/json" \
  -d '{
    "parametersOverride": {
      "iterations": 100
    }
  }'
```

### Получение результатов
```bash
curl -X GET https://api.example.com/v1/projects/proj_123/results
```

📋 Теги API

| Тег           | Описание              |
| ------------- | --------------------- |
| Projects      | Управление проектами  |
| Import        | Импорт OLGA файлов    |
| Calculations  | Запуск расчётов       |
| Results       | Получение результатов |
| Notifications | Подписка на события   |


🔐 Безопасность
Аутентификация: Bearer Token (JWT)

Протокол: HTTPS (обязательно)

Rate Limit: 1000 запросов/час

📦 Поддерживаемые форматы
Импорт: .opi, .tpl, .tab (OLGA)

Экспорт: CSV, JSON

🔄 Асинхронные операции
API использует асинхронный паттерн для длительных операций:

202 Accepted — запрос поставлен в очередь

Webhooks — уведомления при завершении

Status polling — проверка статуса
